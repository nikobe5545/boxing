<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_no_of_rounds_values"
        android:entryValues="@array/pref_no_of_rounds_values"
        android:key="no_of_rounds_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_no_of_rounds_label" />

    <se.beis.boxingrounds.custom.preference.TimePreference
        android:key="timePrefA_Key"
        android:title="@string/pref_length_of_rounds_label"/>

</PreferenceScreen>
